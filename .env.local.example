ANTHROPIC_API_KEY=sk-ant-...

# Optional: persistent data directory (for Railway/Docker volume mounts)
# DATA_DIR=/data

# Admin stats endpoint key (GET /api/admin/stats?key=YOUR_KEY)
# ADMIN_KEY=some-random-secret
